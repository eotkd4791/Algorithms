문자열 
입력받는방법 

1. %s를 이용해서 입력받는방법
scanf("%s",배열이름만);
띄어쓰기 인식못함.
ex) my name is stef
'my'

2. gets라는 함수를 이용한다.
버퍼 오버플로우를 검사를 안한다.
(치명적인 문제 발생)
gets는 그래서 거의 안씀
대용으로 fgets를 쓴다.

3.
scanf("%s")
fgets 
두가지 방법을 대표적으로 쓰지만 scanf가 띄어쓰기를 인식하지 못하기 때문에 fgets를 쓴다.
fgets 도 iostream 헤더 안에 있다. (입,출력 이니까)
fgets(배열이름str,배열크기1000,stdin);
char str[1000];
fgets는 \n줄바꿈 문자까지 포함되어 있기때문에, char 배열을 선언할때 크기를 넉넉하게 잡아줘야한다.
strlen(배열이름);  배열크기가 1000이지만 1000보다 작은 크기로 입력했을때 입력한 크기만이 필요할때 
배열의 크기를 출력하는 함수 strlen()을 이용하여 for문을 돌린다.
strlen(str);
